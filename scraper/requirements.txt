# Production Web Scraping Dependencies
# Install with: pip install -r requirements.txt (or uv pip install -r requirements.txt)

# Async HTTP Client with HTTP/2 and connection pooling
httpx>=0.27.0

# TLS Fingerprint spoofing (Bypasses Cloudflare & JA3/JA4 fingerprinting)
# Note: curl-cffi requires C compiler / prebuilt wheels
curl_cffi>=0.7.0

# High-performance HTML parsing (5x-30x faster than BeautifulSoup)
selectolax>=0.3.21

# Fallback resilient HTML parser
beautifulsoup4>=4.12.0
lxml>=5.2.0

# Robust Data Validation & Typing
pydantic>=2.7.0

# Non-blocking async file I/O for streaming JSONL storage
aiofiles>=23.2.0

# User-Agent rotation helper
fake-useragent>=1.5.1

# Rich terminal logging and progress bars
rich>=13.7.0
tqdm>=4.66.0
